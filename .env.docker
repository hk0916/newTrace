# ============================================================
# TraceTag Platform - Docker 환경 설정 파일
# ============================================================
# 이 파일만 수정하면 서버 환경에 맞게 배포할 수 있습니다.
# 수정 후: docker compose up -d --build
# ============================================================

# ---- 서버 접속 주소 ----
# 서버의 IP 또는 도메인을 입력하세요 (브라우저에서 접속할 주소)
# 예시: 192.168.1.100, tracetag.example.com, localhost
SERVER_HOST=localhost

# ---- 포트 설정 ----
# Next.js 웹 대시보드 포트
WEB_PORT=3000

# WebSocket 서버 포트 (게이트웨이 연결용)
WS_PORT=8080

# PostgreSQL 외부 접속 포트 (DB 관리 도구 연결용, 내부는 항상 5432)
DB_PORT=5432

# ---- 데이터베이스 설정 ----
POSTGRES_USER=tracetag_user
POSTGRES_PASSWORD=tracetag_pass
POSTGRES_DB=tracetag_dev

# ---- NextAuth 인증 ----
# 반드시 변경하세요! (openssl rand -base64 32 로 생성 가능)
NEXTAUTH_SECRET=qMEqq3WXZCkNWgK74lCagO9q5xFSvfePpvU8NMaJX1Y=
